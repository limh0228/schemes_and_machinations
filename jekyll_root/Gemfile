# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# Required for Ruby 3.0 or later because these versions of Ruby no longer come with webrick installed
gem "webrick", "~> 1.9"

# Required for Ruby 3.4 of later because not standard install
gem "bigdecimal"

# Required for pagination (obvi)
gem "jekyll-paginate"
